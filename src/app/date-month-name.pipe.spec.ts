import { DateMonthNamePipe } from './date-month-name.pipe';

describe('DateMonthNamePipe', () => {
  it('create an instance', () => {
    const pipe = new DateMonthNamePipe();
    expect(pipe).toBeTruthy();
  });
});
